import { useState } from "react";

function Game() {

    const [selectedCards, setSelectedCards] = useState(0);

    return (
        <div id="game-wrapper">

        </div>
    );
}

export default Game;